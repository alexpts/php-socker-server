fopen('php://fd/4', 'r+');

воркер процессы могут работать через стримы также и stream_select()

Родитлеьский если не был считан стрим/сокет, то он повторн овернется в stream_select
Можно передавать в воркер процесс число с id fd и воркер процесс считает данные и обслужит дочерний процесс

Можно попробовать открыть даже другой процесс и получить доступ к дескриптору

stream_copy_to_stream - перенаправляет данные межжу потоками


### Попробовать реализациии на базе https://www.php.net/manual/ru/class.eventconfig.php в разных режимах

### Сделать на базе ReactPhp EventLoop посомтреть на его оверхед, это хорошая абстракция для замены низкого уровня